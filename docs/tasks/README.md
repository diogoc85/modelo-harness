# Tasks

Place approved work in `current/` and completed records in `completed/`. Each task should reference
the PRD, define dependencies, acceptance criteria, security considerations and verification commands.
